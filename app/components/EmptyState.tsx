const EmptyState = () => {
    return (
        <div className="h-full w-full flex justify-center items-center bg-onyx bg-opacity-50 rounded-lg">
            <h1 className="text-white">Chat is empty. Start a new conversation now!</h1>
        </div>
    );
}
 
export default EmptyState;